---
- hosts: all
  become: true
  become_user: root
  gather_facts: false

  tasks:
    # Stop local SAP server
    - name: Stop local SAP server
      ansible.builtin.command: /opt/sap/bin/sap_stop
      register: sap_stop_command
      changed_when: sap_stop_command.rc != 0
